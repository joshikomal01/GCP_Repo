steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - hello_world
  - --source=.
  - --trigger-http
  - --runtime=python310
  - --allow-unauthenticated
logsBucket: 'gs://build_storage'
options:
  logging: GCS_ONLY